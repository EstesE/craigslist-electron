<div class="container">
    <div class="row">
        <div class="col-xs-12">
            {{#link-to 'application'}}Home{{/link-to}}
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div>Ad placed for <i>{{model.name}}</i> at <i>{{model.location.name}}, {{model.location.region}}</i>.</div>
        </div>
    </div>

    {{outlet}}

</div>