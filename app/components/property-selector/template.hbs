{{#power-select
  selected=property
  options=properties
  searchField=searchField
  onchange=(action 'test')
  as |property|
}}
  {{property.name}} - ({{property.address.state.abbreviation}} {{property.address.city}})
{{/power-select}}

{{yield}}